#ifndef H_CONSTANTS
#define H_CONSTANTS
#include <cmath>
float degrees(float radians) ;
float radians(float degrees) ;
const int arena_width  = 1200;
const int arena_height  = 1200;
const float scale = 32;
#endif